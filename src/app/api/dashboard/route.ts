import{NextResponse}from"next/server";import{listBookings}from"@/lib/store";export async function GET(){const bookings=await listBookings();return NextResponse.json({data:{total:bookings.length,active:bookings.filter((b)=>!["completed","cancelled"].includes(b.status)).length,completedRevenue:bookings.filter((b)=>b.status==="completed").reduce((sum,b)=>sum+b.estimate,0),recent:bookings.slice(0,5)}})}
