import Link from "next/link";
import { listBookings } from "@/lib/store";
import { formatCurrency, formatDate } from "@/lib/format";
export default async function Dashboard() { const bookings=await listBookings(); const active=bookings.filter((b)=>!["completed","cancelled"].includes(b.status)).length; const revenue=bookings.filter((b)=>b.status==="completed").reduce((sum,b)=>sum+b.estimate,0); return <><div className="titleRow"><div><p className="eyebrow">Operations overview</p><h1>Dashboard</h1></div><Link className="button" href="/bookings/new">Create booking</Link></div><section className="cards" aria-label="Booking summary"><article className="card"><span>Total bookings</span><strong>{bookings.length}</strong></article><article className="card"><span>Active jobs</span><strong>{active}</strong></article><article className="card"><span>Completed revenue</span><strong>{formatCurrency(revenue)}</strong></article></section><section><div className="titleRow"><h2>Recent bookings</h2><Link href="/bookings">View all</Link></div>{bookings.length===0?<p className="empty">No bookings yet.</p>:<div className="tableWrap"><table><thead><tr><th>Customer</th><th>Service</th><th>Status</th><th>Date</th><th>Estimate</th></tr></thead><tbody>{bookings.slice(0,5).map((b)=><tr key={b.id}><td><Link href={`/bookings/${b.id}`}>{b.customerName}</Link></td><td>{b.serviceType}</td><td><span className={`status ${b.status}`}>{b.status}</span></td><td>{formatDate(b.createdAt)}</td><td>{formatCurrency(b.estimate)}</td></tr>)}</tbody></table></div>}</section></>; }
