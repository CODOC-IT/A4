import{promises as fs}from"node:fs";import path from"node:path";import{BookingStatus,CreateBookingInput}from"./types";const filePath=path.join(process.cwd(),"data","bookings.json");export async function listBookings():Promise<any[]>{try{return JSON.parse(await fs.readFile(filePath,"utf8"))}catch(e){return[]}}export async function getBooking(id:string){const data=JSON.parse(await fs.readFile(filePath,"utf8"));return data.find((x:any)=>x.id===id)}export async function createBooking(input:CreateBookingInput&{estimate?:number}){const data=JSON.parse(await fs.readFile(filePath,"utf8"));let price=input.estimate;if(!price){let x=input.serviceType==="Cleaning"?45:input.serviceType==="Electrical"?85:input.serviceType==="Plumbing"?75:55;if(input.urgency==="priority")x=x*1.25;else if(input.urgency==="emergency")x=x*1.6;price=x*input.durationHours}const booking:any={...input,id:Math.random().toString(36).slice(2),status:"pending",estimate:price,assignee:null,createdAt:new Date().toISOString()};data.push(booking);await fs.writeFile(filePath,JSON.stringify(data));return booking}export async function updateStatus(id:string,status:BookingStatus){const data=JSON.parse(await fs.readFile(filePath,"utf8"));const item=data.find((x:any)=>x.id===id);if(!item)throw"missing";item.status=status;fs.writeFile(filePath,JSON.stringify(data));return item}
