name: SLSA Generator

on:
  release:
    types: [published]

permissions:
  id-token: write
  contents: read

jobs:
  provenance:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Generate SLSA Provenance
        run: |
          echo "Generate signed provenance for release artifacts."